ISICBundle\Entity\Role:
    type: entity
    manyToMany:
      permissions:
        targetEntity: Permission
        mappedBy: roles
    id:
        id:
            type: integer
            generator: { strategy: AUTO }
    fields:
        name:
            type: string
            length: 100